# Example dotfiles .env
# Loaded by .zshrc with auto-export (no "export" needed).
# Keep secrets out of git unless you intend to share them.

DOTFILES_ENV=1
OPENAI_API_KEY=your_key_here
GITHUB_TOKEN=your_token_here
PATH_EXTRA=$HOME/bin
