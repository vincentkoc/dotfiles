#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${TMUX_PANE:-}" ]]; then
  echo "TMUX_PANE not set" >&2
  exit 1
fi

# Visual attention without altering status style.
tmux display-panes -d 2000 >/dev/null 2>&1 || true
tmux display-message -d 2000 "attention" >/dev/null 2>&1 || true
